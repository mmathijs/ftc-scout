query OprRecords($season: Int!, $sortDir: SortDir, $skip: Int!, $take: Int!) {
    oprRecords(season: $season, sortDir: $sortDir, skip: $skip, take: $take) {
        count
        offset
        data {
            opr
            epa
            matchesPlayed
            rank
            team {
                number
                name
            }
        }
    }
}
